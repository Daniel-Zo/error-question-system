/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['https://zhlynogiflwfupkvrune.supabase.co'], // 替换成你的 Supabase 存储域名
  },
  reactStrictMode: true,
}

module.exports = nextConfig